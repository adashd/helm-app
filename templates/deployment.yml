apiVersion: apps/v1
kind: Deployment
metadata:
  name: myd1
  labels:
    app: myapp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: c1
        image: adashd/httpd:v1
